import React from "react";

import {
    Container,
    Title,
    Separator,
    Description,
} from "./styles";

const ChannelInfo: React.FC = () => {
    return (
        <Container>
            <Title>Lookup</Title>
            <Separator />
        </Container>
    );
};

export default ChannelInfo;
